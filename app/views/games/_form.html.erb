<%= form_for(game) do |f| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :userid %>
    <%= f.number_field :userid %>
  </div>

  <div class="field">
    <%= f.label :shot_position %>
    <%= f.text_field :shot_position %>
  </div>

  <div class="field">
    <%= f.label :shot_result %>
    <%= f.check_box :shot_result %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
